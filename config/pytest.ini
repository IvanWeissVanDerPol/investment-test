[pytest]
# Auto-load test environment variables for all test runs
env_files = .env.test

# Keep discovery scope aligned with existing pyproject config
testpaths = tests
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*

# Keep other pytest options in pyproject.toml; this file only handles env loading.
